"use client";  import Image from "next/image"; import Link from "next/link"; import { usePathname } from "next/navigation"; import { useState } from "react";  import { getUiCopy, localizedPath, type Locale } from "@/lib/i18n"; import { isActivePath, normalizePath } from "@/lib/path-utils"; import { getHeaderRoutes, getRouteHref } from "@/lib/site-routes"; import { companyProfile } from "@/lib/site-data"; import { ThemeToggle } from "@/components/theme-toggle"; import { HeaderShortcuts } from "@/components/header-shortcuts";  type SiteHeaderProps = {   locale: Locale; };  export function SiteHeader({ locale }: SiteHeaderProps) {   const copy = getUiCopy(locale);   const pathname = usePathname();   const [menuOpen, setMenuOpen] = useState(false);   const alternateLocale = locale === "ar" ? "en" : "ar";   const switchedPath = pathname     ? pathname.replace(/^\/(ar|en)(?=\/|$)/, `/${alternateLocale}`)     : `/${alternateLocale}`;   const homeHref = localizedPath(locale);   const currentPath = normalizePath(pathname ?? homeHref);   const links = getHeaderRoutes().map((route) => ({     label: copy.nav[route.key],     href: getRouteHref(locale, route),   }));    return (     <header className="site-header sticky top-0 z-50 overflow-hidden border-b backdrop-blur-xl">       <span         key={currentPath}         aria-hidden         className="route-transition-bar"         style={{           transformOrigin: locale === "ar" ? "right center" : "left center",         }}       />       <div className="container-shell flex items-center justify-between gap-4 py-4">         <Link href={localizedPath(locale)} className="flex items-center gap-3">           <div className="site-header-logo-box relative h-12 w-12 overflow-hidden rounded-2xl">             <Image               src="/media/logo.png"               alt="Image Investments"               fill               sizes="48px"               className="object-contain p-1"             />           </div>           <div className="text-[var(--header-text)]">             <div className="english-accent text-xs uppercase tracking-[0.35em] text-[var(--color-gold-bright)]">               Image Investments             </div>             <div className="text-sm font-bold text-[var(--header-text-strong)]">               ايمدج للاستثمارات             </div>           </div>         </Link>          <nav className="site-nav-shell hidden items-center gap-1 rounded-full border px-2 py-2 lg:flex">           {links.map((item) => {             const active = isActivePath(currentPath, item.href, homeHref);              return (               <Link                 key={item.href}                 href={item.href}                 className={[                   "site-nav-link rounded-full px-4 py-2 text-sm font-semibold",                   active                     ? "site-nav-link-active bg-[var(--color-gold)] text-[var(--color-navy)]"                     : "",                 ].join(" ")}               >                 {item.label}               </Link>             );           })}         </nav>          <div className="hidden items-center gap-3 lg:flex">           <HeaderShortcuts locale={locale} />           <ThemeToggle locale={locale} />           <Link             href={switchedPath}             className="site-header-secondary rounded-full border px-4 py-2 text-sm font-semibold"           >             {copy.languageSwitch}           </Link>           <a             href={companyProfile.whatsappHref}             target="_blank"             rel="noreferrer"             className="rounded-full bg-[var(--color-gold)] px-5 py-2.5 text-sm font-semibold text-[var(--color-navy)] hover:-translate-y-0.5 hover:bg-[var(--color-gold-bright)]"           >             {copy.actions.whatsapp}           </a>         </div>          <button           type="button"           aria-expanded={menuOpen}           aria-label={locale === "ar" ? "فتح أو إغلاق القائمة" : "Toggle navigation"}           onClick={() => setMenuOpen((current) => !current)}           className="site-header-menu-button inline-flex h-11 w-11 items-center justify-center rounded-full border lg:hidden"         >           <span className="text-xl leading-none">{menuOpen ? "×" : "≡"}</span>         </button>       </div>        {menuOpen ? (         <div className="mobile-menu-enter site-mobile-menu border-t lg:hidden">           <div className="container-shell flex flex-col gap-2 py-4">             {links.map((item) => {               const active = isActivePath(currentPath, item.href, homeHref);                return (                 <Link                   key={item.href}                   href={item.href}                   onClick={() => setMenuOpen(false)}                   className={[                     "site-mobile-link rounded-2xl border px-4 py-3 text-sm font-semibold",                     active                       ? "border-[var(--color-gold)] bg-[rgba(205,168,109,0.2)] text-[var(--color-gold-bright)]"                       : "",                   ].join(" ")}                 >                   {item.label}                 </Link>               );             })}             <div className="mt-2 flex flex-col gap-2">               <ThemeToggle locale={locale} compact />               <Link                 href={switchedPath}                 onClick={() => setMenuOpen(false)}                 className="site-mobile-link rounded-2xl border px-4 py-3 text-sm font-semibold"               >                 {copy.languageSwitch}               </Link>               <a                 href={companyProfile.whatsappHref}                 target="_blank"                 rel="noreferrer"                 className="rounded-2xl bg-[var(--color-gold)] px-4 py-3 text-center text-sm font-semibold text-[var(--color-navy)]"               >                 {copy.actions.whatsapp}               </a>             </div>           </div>         </div>       ) : null}     </header>   ); }