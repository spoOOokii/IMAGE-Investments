import { notFound } from "next/navigation";  import { LeadForm } from "@/components/lead-form"; import { PageHero } from "@/components/page-hero"; import { PropertyCard } from "@/components/property-card"; import { SectionHeading } from "@/components/section-heading"; import { buildMetadata } from "@/lib/seo"; import { getFeaturedDeals } from "@/lib/cms"; import { featuredDealHighlights } from "@/lib/site-data"; import { isLocale, pickLocale } from "@/lib/i18n";  export const dynamic = "force-dynamic";  export async function generateMetadata({   params, }: {   params: Promise<{ locale: string }>; }) {   const { locale } = await params;    if (!isLocale(locale)) {     return {};   }    return buildMetadata({     locale,     path: "/deals",     title:       locale === "ar"         ? "أفضل الصفقات | فرص عقارية مميزة"         : "Featured Deals | Curated Property Opportunities",     description:       locale === "ar"         ? "اكتشف أفضل الفرص العقارية المحدودة داخل أهم المناطق المصرية."         : "Explore premium and limited real estate opportunities across Egypt's top locations.",   }); }  export default async function DealsPage({   params, }: {   params: Promise<{ locale: string }>; }) {   const { locale } = await params;    if (!isLocale(locale)) {     notFound();   }    const deals = await getFeaturedDeals();    return (     <>       <PageHero         eyebrow={locale === "ar" ? "أفضل الصفقات" : "Featured Deals"}         title={           locale === "ar"             ? "فرص عقارية مميزة وذات قابلية حركة سريعة"             : "High-conviction opportunities with strong movement potential"         }         description={           locale === "ar"             ? "هذه الصفحة مخصصة للعملاء الذين يركزون على الندرة وجودة الاختيار وقوة التفاوض."             : "This page is built for buyers who care about scarcity, listing quality, and negotiation edge."         }       />        <section className="container-shell pt-18">         <SectionHeading           eyebrow={locale === "ar" ? "ميزة الصفقة" : "Deal Edge"}           title={             locale === "ar"               ? "لماذا نعتبر بعض الوحدات فرصًا حقيقية؟"               : "Why some listings truly qualify as special opportunities"           }           description={             locale === "ar"               ? "ليست كل وحدة معروضة فرصة جيدة. نحن نقيّم السياق، السيولة، وخيارات الخروج قبل الترشيح."               : "Not every listing is a smart deal. We evaluate context, liquidity, and exit strength before recommending it."           }         />         <div className="grid gap-6 md:grid-cols-3">           {featuredDealHighlights.map((item) => (             <div key={item.title.en} className="luxury-surface p-6">               <div className="text-sm font-bold text-[var(--color-gold)]">                 {locale === "ar" ? "أفضلية" : "Advantage"}               </div>               <h3 className="mt-3 text-2xl font-bold text-[var(--color-ink)]">                 {pickLocale(item.title, locale)}               </h3>               <p className="mt-3 text-sm leading-7 text-[var(--color-ink-soft)]">                 {pickLocale(item.description, locale)}               </p>             </div>           ))}         </div>       </section>        <section className="container-shell pt-18">         <SectionHeading           eyebrow={locale === "ar" ? "وحدات محدودة" : "Limited Listings"}           title={             locale === "ar"               ? "أفضل الوحدات المتاحة الآن"               : "The strongest listings available right now"           }           description={             locale === "ar"               ? "اخترنا الوحدات التي تمتلك ميزة واضحة للمشتري أو المستثمر من زاوية الجودة أو الندرة."               : "These are the units with the clearest quality or scarcity advantage for buyers and investors."           }         />         <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">           {deals.map((property) => (             <PropertyCard key={property.slug} locale={locale} property={property} />           ))}         </div>       </section>        <section className="container-shell py-18">         <LeadForm           locale={locale}           source="deals-page"           title={             locale === "ar"               ? "اطلب أفضل الصفقات بحسب ميزانيتك"               : "Request the best deals based on your budget"           }           description={             locale === "ar"               ? "أرسل لنا نوع العقار والمنطقة المستهدفة لنرشح لك أفضل الوحدات المناسبة."               : "Tell us the target property type and location and we will shortlist the strongest opportunities."           }         />       </section>     </>   ); }