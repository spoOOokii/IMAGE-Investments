import { notFound } from "next/navigation";  import { LeadForm } from "@/components/lead-form"; import { PageHero } from "@/components/page-hero"; import { PropertyCard } from "@/components/property-card"; import { SectionHeading } from "@/components/section-heading"; import { buildMetadata } from "@/lib/seo"; import { getFeaturedProperties } from "@/lib/cms"; import { investmentHighlights } from "@/lib/site-data"; import { isLocale, pickLocale } from "@/lib/i18n";  export async function generateMetadata({   params, }: {   params: Promise<{ locale: string }>; }) {   const { locale } = await params;    if (!isLocale(locale)) {     return {};   }    return buildMetadata({     locale,     path: "/investment-opportunities",     title:       locale === "ar"         ? "الفرص الاستثمارية | استثمار عقاري في مصر"         : "Investment Opportunities | Real Estate Investment in Egypt",     description:       locale === "ar"         ? "فرص استثمار عقاري في مصر تشمل إعادة البيع، المشروعات البريميوم، والوحدات المصيفية."         : "Explore Egyptian real estate investment opportunities across resale deals, premium compounds, and seasonal properties.",   }); }  export default async function InvestmentPage({   params, }: {   params: Promise<{ locale: string }>; }) {   const { locale } = await params;    if (!isLocale(locale)) {     notFound();   }    const featuredProperties = await getFeaturedProperties();    return (     <>       <PageHero         eyebrow={locale === "ar" ? "الفرص الاستثمارية" : "Investment Opportunities"}         title={           locale === "ar"             ? "استثمار عقاري مبني على القراءة الذكية للسوق"             : "Real estate investment built on sharper market reading"         }         description={           locale === "ar"             ? "نساعدك على اكتشاف فرص إعادة البيع والمشروعات البريميوم داخل أسواق مصر الأكثر نموًا."             : "We help you identify premium compounds and resale opportunities across Egypt's strongest growth markets."         }       />        <section className="container-shell pt-18">         <SectionHeading           eyebrow={locale === "ar" ? "لماذا العقار؟" : "Why Real Estate?"}           title={             locale === "ar"               ? "أصل يحفظ القيمة ويتيح أكثر من سيناريو للعائد"               : "An asset class that protects value and offers multiple return paths"           }           description={             locale === "ar"               ? "الشراء الذكي لا يعتمد فقط على اسم المشروع، بل على توقيت الدخول، قوة الموقع، وسيناريو الخروج."               : "Smart property investing is not only about the project name, but also timing, location strength, and exit scenario."           }         />         <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-4">           {investmentHighlights.map((item) => (             <div key={item.title.en} className="luxury-surface p-6">               <div className="text-sm font-bold text-[var(--color-gold)]">                 {locale === "ar" ? "فرصة" : "Opportunity"}               </div>               <h3 className="mt-3 text-2xl font-bold text-[var(--color-ink)]">                 {pickLocale(item.title, locale)}               </h3>               <p className="mt-3 text-sm leading-7 text-[var(--color-ink-soft)]">                 {pickLocale(item.description, locale)}               </p>             </div>           ))}         </div>       </section>        <section className="container-shell pt-18">         <SectionHeading           eyebrow={locale === "ar" ? "وحدات مقترحة للمستثمرين" : "Investor Picks"}           title={             locale === "ar"               ? "ترشيحات مختارة للعائد والسيولة"               : "Selected opportunities for return and liquidity"           }           description={             locale === "ar"               ? "مزيج بين الوحدات الجاهزة، الفرص المميزة، والمشروعات ذات الطلب القوي."               : "A mix of ready inventory, curated opportunities, and projects with strong demand fundamentals."           }         />         <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">           {featuredProperties.slice(0, 6).map((property) => (             <PropertyCard key={property.slug} locale={locale} property={property} />           ))}         </div>       </section>        <section className="container-shell py-18">         <LeadForm           locale={locale}           source="investment-page"           theme="dark"           title={             locale === "ar"               ? "اطلب عرضًا استثماريًا مناسبًا لميزانيتك"               : "Request an investment shortlist matched to your budget"           }           description={             locale === "ar"               ? "أخبرنا بالمنطقة المستهدفة، نوع الأصل، وأفق الاستثمار لنرشح لك أفضل الفرص المتاحة."               : "Share your target location, asset type, and investment horizon so we can recommend the best available opportunities."           }         />       </section>     </>   ); }